[DONE 2026-06-29] Fate sets the spread is bugged, hand does not go away after animation ends.
   - Root cause: the Fate-force sequence in MethodSelect (freeze→press→reject→ascend→idle + confirmSelection) is driven by a useEffect keyed on the `pending` object. notify() deep-clones state (JSON round-trip, GameEngine.ts:124), so `pending` gets a NEW reference on every notify. Any unrelated notify() landing inside the ~2.6s sequence — at virulent+, the most common is IntrusionOverlay's clearIntrusion() timer (fires 2400ms after an intrusion that carried over from the prior reading's minigame) — re-ran the effect; its cleanup cleared the in-flight timers while `confirmedRef.current === true` short-circuited the reschedule, so setForceStage('idle') and confirmSelection() never fired and the hand froze on screen. Matches the "timing/edge case on a rupturing reading" hunch.
   - Fix: depend on the *stable* identity of the selection — `[nonce, pending != null, engine]` — instead of the cloned `pending` object. nonce + presence stay constant for the whole sequence, so reference churn from notify() no longer re-runs the effect. One-line dep change in MethodSelect.tsx; tsc clean, all 670 engine tests green. (No component test — vitest is engine-only by project convention; verify visually with a virulent-band fate-force.)

[DONE 2026-06-29] Card draw animation for corruption is bugged, it should ascend in its original position instead of moving to the center.
   - Root cause: infectedAscendOverlayStyle (MethodSelect.tsx) centered the CorruptedAscend with flex align/justify-center, so the shear/ascend played from the middle of the spread.
   - Fix: onPick now also measures the picked card's slot relative to the spread wrapper (spreadWrapRef) — left/top/width/height stored in infectedAscendRect — and the overlay places the CorruptedAscend absolutely at that slot (centre fallback if unmeasured). The .cx-ca-ascend translateY now lifts the card in place. The overlay layer dropped its flex centering. tsc clean; engine suite green (vitest is engine-only, so verify the ascend visually).

[DONE 2026-06-29] There should be a slight hint at method card glitching in the spreading phase if you look carefully.
   - The .cx-card-spreading class was already applied at spreading (MethodSelect.corruptedFor) but only had a slow chromatic pulse on the title — no "jump." Added a faint ~6.3s single twitch on .cx-card-spreading .cx-glyph (cx-jump-faint in corruption.css): easy to miss unless you're watching for it; virulent still jitters harder.

[FLAGGED — future session, medium] Check if the early warning system for light is implemented. Currently do not see any warning.
   - It IS implemented in the engine: state.corruptionWarning { present, tainted, methods } via deriveCorruptionWarning (covered by CorruptionWarning.test.ts). But NO component reads it — that's why nothing shows. Needs a UI surface (likely on MethodSelect): a vague warning at Ascendant Light, named/highlighted tainted cards at Dominant Light, and a "tainted" (unreliable) warning once corruption is Virulent. Engine side is ready; this is purely presentation.

[DONE 2026-06-29] Corrupted result page — match corrupted-reading.html more faithfully. Guiding principle from the reference: legibility first — you can always read the lie. The dread comes from CSS-styled inline spans wrapping words that STAY readable, not from destroying the text. The current implementation leans too hard on content-destroying redaction (█ blocks) + heavy garble, and appends the whispers as plain unstyled sentences so they don't register.
   - Redaction is way too heavy at early virulent. Reserve heavy/multiple redactions for late-stage virulent (near pinnacle). Early virulent = at most ONE redaction; drop the per-word redaction probability hard (currently ~0.18/word).
   - Early virulent (just past threshold) = legible but clearly defiled: a chromatic-aberration pulse (.ca) on a word or two, ONE redaction, one readable stutter ("new — new — new"), faint scanlines + occasional red datamosh slices. Spreading-stage subtle errors (typos, tone drift, detail swaps) still underneath.
   - Near pinnacle = everything ramps: heavier/faster chromatic (.ca.fast + .hot glow), light zalgo accents on select words, multiple redactions, repeats/stutters, contradictory fragments, dense datamosh. Still readable.
   - Whispers: render the entity intrusions as inline GHOST spans (.ghost — pink, italic, low-opacity), parenthetical and woven mid-paragraph, e.g. "(the way ahead does not exist)", "The card you did not draw speaks the loudest.", "Expect us." — NOT appended as plain sentences. Right now they blend in / don't read as whispers; verify they actually render on the results page.
   - Add the missing legibility-preserving primitives from the reference's cx kit so corruption isn't all redaction+garble: .red (recolor a word red), .flick (flicker), .hot (white-hot glow), .stut (readable stutter), .ca (chromatic pulse). These are span-level styles — the underlying word stays legible.
   - Implementation implication: shift from the current destructive text rewrite (corruptText emitting █ blocks / inline combining-char garble) toward emitting marked-up segments (word + style class) that ResultReading renders as styled spans.

[DONE 2026-06-29] I also would like pinnacle to be triggered after exiting the results page, so that the user can take one last look at how bad it got before it reset.
   - Root cause: advanceAfterCommit's rupture guard routed to screen='rupture' BEFORE the final-reading guard, so a rupture on the FINAL reading skipped the result screen entirely.
   - Fix: split flag-from-wipe. applyCorruptionTick now only sets pendingRupture (performRupture no longer self-flags). advanceAfterCommit ruptures immediately ONLY mid-turn; on the final reading it leaves pendingRupture armed, synthesizes + buildRunRecord at PEAK corruption (result renders fully defiled), and routes to 'result'. returnToQuestionSelect (DRAW AGAIN) fires the held-back performRupture and routes to 'rupture' then. maybeIntrude is gated on !pendingRupture so a rupturing turn never also intrudes. New CorruptionLifecycle test covers the deferral (existing mid-turn rupture tests use GameEngine(3)+1 reading, so they were unaffected); docs/game-systems.md Rupture-interstitial section updated. All 671 engine tests green; tsc clean.

[DONE 2026-06-29] The whispers from the entity that shows as a popup should be more obvious that it's there in the virulent stage.
   - IntrusionOverlay renders .cx-intrusion. Made it announce itself: a brief red wash blooms behind the phantom line (cx-intr-wash) and the text is larger / heavier / harder-glowing (corruption.css), instead of blending into the UI.

[NEW, medium] Check if things like forbidden sight and the gameplay effects of corruption are wired up. I currently do not see any gameplay elements besides the cosmetic things.
